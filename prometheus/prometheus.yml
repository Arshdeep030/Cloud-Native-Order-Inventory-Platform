global:
  scrape_interval: 5s

scrape_configs:

  - job_name: "order-api"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "api:8000"

  - job_name: "gateway"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "gateway:8000"
